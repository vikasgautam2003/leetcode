    {
        if(sum == target)
        {
            arr.add(res);
            return;
        }

        int ans = candidates[index]
        res.add();
        helper(candidates, target, arr, res, index + 1, sum + can);